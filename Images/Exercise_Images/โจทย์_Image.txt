จำโปรแกรม picture generator ที่คุณเขียนไว้ก่อนหน้านี้
ได้ไหม? ลองเขียนโปรแกรมอีกครั้ง
แต่คราวนี้มันจะคืนค่าการใช้งาน image.Image แทนที่จะเป็น
ข้อมูลแบบ slice

กำหนดประเภท Image ของคุณเอง, ใช้งาานเมธอดที่จำเป็น
และเรียก pic.ShowImage
เมธอด Bounds ควรคืนค่า image.Rectangle
เช่น image.Rect(0, 0, w, h)
เมธอด At ควรคืนค่าสี; ค่าของ v ในโปรแกรมสร้างภาพครั้งที่
แล้วจะสอดคล้องกับ color.RGBA{v, v, 255, 255} ใน
โปรแกรมนี้
