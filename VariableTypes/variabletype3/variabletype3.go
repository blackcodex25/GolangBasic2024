package main

import (
	f "fmt"
)

func main() {
	var student1 string //สร้างตัวแปร student1 และกำหนดชนิดข้อมูล string
	student1 = "John"   // กำหนดค่าตัวแปรให้ student1 ในภายหลังด้วยการใช้ = โดยกำหนดค่าเป็น John
	f.Println(student1) // ปริ้นค่าของ student1 ที่ได้รับค่าเป็น "John"
}
