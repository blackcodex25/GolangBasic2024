package main

import (
	f "fmt"
)

func main() {
	var a string
	var b int
	var c bool
	f.Println(a) // ปริ้นค่าของ a ซึ่งมีค่าเริ่มต้นเป็น ""
	f.Println(b) // ปริ้นค่าของ b ซึ่งมีค่าเริ่มต้นเป็น 0
	f.Println(c) // ปริ้นค่าของ c ซึ่งมีค่าเริ่มต้นเป็น false
}
