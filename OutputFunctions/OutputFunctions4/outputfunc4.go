package main

import (
	f "fmt"
)

/*
ฟังก์ชัน Print() ใน Go กับอาร์กิวเมนต์ที่ไม่ใช่สตริง
ฟังก์ชัน Print()
ใน Go จะเพิ่มช่องว่างระหว่างอาร์กิวเมนต์ถ้าอาร์กิวเมนต์ทั้งสองไม่ใช่สตริง
*/
var i, j = 10, 20

func main() {
	f.Print(i, j) //ปริ้นหลายตัวแปรในบรรทัดเดียว
}
